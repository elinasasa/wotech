# LESSON 38 28.08

In order to send the repository request to DB, you need a "Driver", in the connection you send a querry (the join, left join etc.) its all in one connection. DB has limited number of connections. Repository runs requests. Important to after making the connection you close the request. 
"start connection - updat data - close connection."

When is the DB created?

```
CREATE TABLE persons 

(id Integer primary key, name varchar(20));
```
now the name has 20 char.limit

varchar is a string

"Auto increment" adds just next number:

```
1. Oskars
2. Anna
3. Laila
4. Andrey

Delete where id = 3

1. Oskars
2. Anna
4. Andrey

Add Laila

1. Oskars
2. Anna
4. Andrey
5. Laila

Delete where id = 5

1. Oskars
2. Anna
4. Andrey

Add Laila again

1. Oskars
2. Anna
4. Andrey
6. Laila
```

Inserting:

```
INSERT INTO persons (name) VALUES (
  'oskars'
); 
```


ORM - whats used to create/access DB. nowadays noone creates DB table manually

### TEAMWORK:

Download dbBrowser for sqlite
https://sqlitebrowser.org/dl/

Try to open the database that you created in your project (you can see the my.db in files on the left side)

Read about SQL, learn insert, delete, update, select on this page

https://sqliteonline.com/

A lot of resources is here
https://www.w3schools.com/sql/default.asp

Create 3 tables with some non-complex data, populate them with values, you will be able to use this database later on, but this is just for you to practise and understand db a bit better.

helps to see whats going on in the DB.

# LESSON 39, 29.08

```
import java.sql.DriverManager;
import java.sql.SQLException;


public class Main {

    public static void main(String[] args) {
        String url = "jdbc:sqlite:my.db";

        try (var conn = DriverManager.getConnection(url)) {
            if (conn != null) {
                var meta = conn.getMetaData();
                System.out.println("The driver name is " + meta.getDriverName());
                System.out.println("A new database has been created.");
            }
        } catch (SQLException e) {
            System.err.println(e.getMessage());
        }
    }
}
```
this code is copied from google and it helps to opent he db??

try and catch - if any of those actions fails, we start doing smth else. its like try/except in Python.

creating a table:

```
import java.sql.DriverManager;
import java.sql.SQLException;


public class Main {

    public static void main(String[] args) {
        String url = "jdbc:sqlite:my.db";
        try (var conn = DriverManager.getConnection(url)) {
            if (conn != null) {
                var statement = conn.createStatement(); // connecting to DB
                statement.execute("CREATE TABLE people (id INTEGER PRIMARY KEY AUTOINCREMENT, name varchar(20))");
            }
        } catch (SQLException e) {
            System.err.println(e.getMessage());
        }
    }
}
```

inserting data:

```
import java.sql.DriverManager;
import java.sql.SQLException;


public class Main {

    public static void main(String[] args) {
        String url = "jdbc:sqlite:my.db";
        try (var conn = DriverManager.getConnection(url)) {
            if (conn != null) {
                var statement = conn.createStatement(); // connecting to DB
                statement.execute("INSERT INTO people (name) VALUES (\n" +
                        "  'anna'\n" +
                        "); ");
            }
        } catch (SQLException e) {
            System.err.println(e.getMessage());
        }
    }
}
```

Then open DB Browser, open file my.db, and ander Browser data you see what was added.


```
import java.sql.DriverManager;
import java.sql.SQLException;
import java.util.Scanner;


public class Main {

    public static void main(String[] args) {
        var sc = new Scanner(System.in);

        // asking for persons name
        System.out.println("PLease provide your name:");
        var personsName = sc.nextLine();


        String url = "jdbc:sqlite:my.db";
        try (var conn = DriverManager.getConnection(url)) {
            if (conn != null) {
                var statement = conn.createStatement();
                statement.execute("INSERT INTO people (name) VALUES ('" + personsName + "')");
            }
        } catch (SQLException e) {
            System.err.println(e.getMessage());
        }
    }
}
```
adding DB for people like this is now dangerous and not done anymore. its too easy to destroy a DB.

### SQL injection

https://portswigger.net/web-security/sql-injection

if writing code in places where one shouldn't, easy to break the programme. 
